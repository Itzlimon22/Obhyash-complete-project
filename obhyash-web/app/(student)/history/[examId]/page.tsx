// /history/[examId] → renders the full SPA shell.
// StudentRoot reads the URL on mount and shows the exam result view,
// loading the exam from history by the examId in the path.
import DashboardPage from '../../dashboard/page';
export default DashboardPage;
