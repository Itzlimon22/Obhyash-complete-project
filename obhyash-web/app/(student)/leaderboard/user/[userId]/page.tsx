// /leaderboard/user/[userId] → renders the full SPA shell.
// StudentRoot reads the URL on mount and shows the user_profile tab,
// fetching the profile by the userId in the path.
import DashboardPage from '../../../dashboard/page';
export default DashboardPage;
